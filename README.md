# Secure Password Generator

A simple but secure command-line password generator built as a learning project.

## Features
- Generates random passwords between 8–20 characters
- Uses only letters (A-Z, a-z) and numbers (0-9)
- Uses cryptographically secure randomness (`secrets` module)
- Clean, readable terminal output with security warning

## How to Run

```bash
py password_generator.py
