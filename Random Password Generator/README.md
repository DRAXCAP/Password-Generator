# Secure Password Generator

A simple, secure command-line password generator (v1.1).

## Features
- Generates a random password between 8 and 20 characters
- Uses only letters (A-Z, a-z) and numbers (0-9)
- Uses cryptographically secure randomness (`secrets` module)
- Clean, readable output

## How to Run

```bash
py password_generator.py